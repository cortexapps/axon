{
  "private": [
    {
      "method": "any",
      "path": "/*",
      "origin": "${PROMETHEUS_API}",
      "auth": {
          "scheme": "basic",
          "username": "${PROMETHEUS_USERNAME}",
          "password": "${PROMETHEUS_PASSWORD}"
      }
    }
  ]
}
