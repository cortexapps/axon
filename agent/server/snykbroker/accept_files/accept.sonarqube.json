{
  "private": [
    {
      "method": "any",
      "path": "/*",
      "origin": "${SONARQUBE_API}",
      "auth": {
        "scheme": "bearer",
        "token": "${SONAR_TOKEN}"
      }
    }
  ]
}
