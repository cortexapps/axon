{
  "private": [
    {
      "method": "any",
      "path": "/*",
      "origin": "${JIRA_API}",
      "auth": {
          "scheme": "basic",
          "username": "${JIRA_USERNAME}",
          "password": "${JIRA_TOKEN}"
      }
    }
  ]
}
